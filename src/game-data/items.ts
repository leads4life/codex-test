import type{Item}from'../types/game';export const items:Item[]=['brass-key','red-lens','torn-note','photograph','metal-gear','pocket-watch','cipher-disk','projector-slide','small-mirror','mechanical-handle'].map(id=>({id,name:id.split('-').map(w=>w[0].toUpperCase()+w.slice(1)).join(' '),image:`/images/items/${id}.svg`,description:`A tactile ${id.replaceAll('-',' ')} from the inventor's study. Rotate, inspect, combine, or try it on compatible hotspots.`}));
